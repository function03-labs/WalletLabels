query GetContactActivities($id: String!) {
  activities(contactId: $id) {
    id
    user {
      id
      firstName
      lastName
      name
      avatar
      status
    }
    type
    data
    date
    createdAt
    updatedAt
  }
}
