mutation UpdateUser($id: String!, $name: String, $email: String) {
  updateUser(id: $id, name: $name, email: $email) {
    id
    name
    email
    organizations {
      id
      name
      slug
      plan
    }
  }
}
