mutation InviteToOrganization(
  $emails: [String]!
  $organizationId: String!
  $role: String
) {
  inviteToOrganization(
    emails: $emails
    organizationId: $organizationId
    role: $role
  )
}
