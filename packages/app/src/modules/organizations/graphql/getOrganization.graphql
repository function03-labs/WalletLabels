query GetOrganization($id: String, $slug: String) {
  organization(id: $id, slug: $slug) {
    id
    name
    slug
    plan
    members {
      roles
      user {
        id
        name
        email
        status
      }
    }
  }
}
