mutation UpdateUser($name: String, $userId: String!) {
  updateUser(name: $name, userId: $userId) {
    id
    name
    email
    organizations {
      id
      name
      slug
      plan
    }
  }
}
