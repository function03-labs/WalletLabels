import { Meta } from '@storybook/react'
import { InlineSearch } from './'

export default {
  title: 'Components/InlineSearch',
  component: InlineSearch,
} as Meta

export const Default = {
  args: {},
}
